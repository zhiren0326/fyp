import 'package:flutter/material.dart';

class ReportProblemPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Report a problem'),
      ),
      body: Center(
        child: Text('Report a Problem Page Content'),
      ),
    );
  }
}