import 'package:flutter/material.dart';

class TermsPoliciesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Terms and Policies'),
      ),
      body: Center(
        child: Text('Terms and Policies Page Content'),
      ),
    );
  }
}